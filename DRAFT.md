# Seam

Seamは、プログラムの実行経路（Execution Path）を静的型付けの対象とする、新しいシステムプログラミング言語です。

従来のプログラミング言語における検証および型システムの主たる対象は「値」ですが、Seamでは「実行経路」そのものに契約（Contract）を与え、コンパイル時に型付け（Path Typing）を行います。プログラムの制御フローがどのような軌跡をたどり、どのような状態遷移を引き起こすかを静的に規定・検証することで、プログラムの実行経路に関する安全性をコンパイル時に保証します。

---

## 1. Basic Syntax

### 1.1 primitive

データの最も基礎的な要素となるプリミティブ型をサポートします。
サポート対象はC言語のプリミティブ型と同等ですが、プラットフォームに依存しない厳密なサイズセマンティクスが規定されています。

| 型名 | ビット幅/定義 | 説明 |
| --- | --- | --- |
| `void` | 0-bit | 値を持たないことを示す型 |
| `bool` | 1-bit | 真偽値（`true` または `false`） |
| `byte` / `ubyte` | 8-bit | 符号あり/符号なし 8ビット整数 |
| `short` / `ushort` | 16-bit | 符号あり/符号なし 16ビット整数 |
| `int` / `uint` | 32-bit | 符号あり/符号なし 32ビット整数 |
| `long` / `ulong` | 64-bit | 符号あり/符号なし 64ビット整数 |
| `float` | 32-bit | IEEE 754 単精度浮動小数点数 |
| `double` | 64-bit | IEEE 754 倍精度浮動小数点数 |
| `char` | 8-bit | 文字データ |
| `string` | 可変長 | 不変（Immutable）な文字列型 |

---

### 1.2 record

プリミティブ型および他のレコード型を任意に組み合わせ、複合的なデータ構造を定義する「レコード型（`record`）」をサポートします。

レコード型は完全な不変（Immutable）であり、レコード型として定義されたフィールドはすべて暗黙的に `final` となります。そのため、インスタンス生成後にフィールドの値を直接書き換えるような破壊的操作は一切認められません。レコード型の値を更新する際は、既存のレコードをベースとして変更したいフィールドのみを上書きした、新しいレコードインスタンスを再生成する「コピー・オン・ライト」のセマンティクスによって行います。これにより、データ共有時の副作用が排除され、安全な参照が保証されます。

```seam
record Data {
    int num;
    string str;
    Data next;
}

```

---

### 1.3 resource

プリミティブ型およびレコード型の組み合わせによって、動的に変化するシステムやプログラムの状態を保持する「リソース型（`resource`）」をサポートします。

不変であるレコード型とは対照的に、リソース型は可変（Mutable）**であり、定義されたフィールドはすべて `var` となります。
このリソース型は、メモリ上の共有領域やハードウェア、ネットワークソケットなどの「状態を伴う対象」を表すために使用されます。そのため、後述の `channel` 型の内部でリソース型を利用する際には、**「どう利用するか」という契約（Requires Contract）を静的に明記しなければなりません。

契約には、リソースの値を読み取る `read` と、書き込みを行う `write` の2種類が存在します。

```seam
requires {
    read { <resourceType>.<MemberName>; }
    write { <resourceType>.<MemberName>; }
}

```

これらのアクセス契約は、コンパイラによる静的エフェクト解析に利用されます。例えば、後述の `fork` 構文などの並行実行パスにおいて「どのパスとどのパスが同じリソースに競合アクセスするか」をコンパイル時に判定し、自動的なロック（排他制御）の必要性を決定する他、変更に伴う影響範囲の静的な特定に役立てられます。

```seam
resource Resource {
    int num;
    string str;
    Resource next;
}

```

---

### 1.4 channel

`record` または `primitive` を引数として受け取り、`record` または `primitive` を返す、実行経路の制御単位である「チャンネル型（`channel`）」をサポートします。
他言語における「関数」や「サブルーチン」に相当する役割を持ちますが、Seamにおけるチャンネルは、経路安全性とエラー回復（リカバリ）のための極めて特異な構造を備えています。

チャンネルには、制御フローのエントリーポイントとして `entry` と `collector` の2つが定義されます。

#### 1. エントリポイント：`entry`

チャンネルが呼び出された際に実行される主たる制御パスです。呼び出し側からは、他言語の関数呼び出しと同様に `ChannelName(args)` の形式で呼び出すことができます。`entry` パス内の処理は、正常終了を示す `return`、または経路の異常（実行継続の不能）を示す `abort` に達することで、その実行コンテキスト（アクティベーション）を終了します。

#### 2. コレクタ：`collector`

`collector` は、呼び出しスタックの途中で発生した `abort` を捕捉し、破綻したシステム状態のクリーンアップやロールバック（回復）を担う特殊なパスです。

Seamには、一般的な言語に見られる **Stack Unwind（スタックの巻き戻し・展開）の概念が存在しません。**
C++やJava等の例外処理機構（スタックアンワインド）は、例外発生時にスタックを1フレームずつ巻き戻しながらデストラクタやクリーンアップ処理を動的に探索・実行するため、実行時（ランタイム）に大きなオーバーヘッドを伴います。

Seamでは、チャンネル呼び出し時にスタックに対して `:collect <RecoveryChannel>` を付加することで、あらかじめ静的に回復経路をバインドします。
もし実行中に `abort` が発生した場合、VMはスタックを1つずつ解体していくのではなく、**`:collect` をスタックに付加した親の呼び出し階層へと、スタックポインタ（SP）を直接ジャンプさせます。** ジャンプ先で、指定されたコレクタ（`collector`）を直ちに呼び出し、状態の回復（再評価）を行います。

この「スタックアンワインドを伴わないダイレクトジャンプ」と「コレクタによる回復後の再評価・制御再開」という独自のセマンティクスを実現するため、Seamは専用のランタイムVM（仮想マシン）を必要とし、C言語のように単純なコールスタックモデルを持つネイティブコードへと直接コンパイルすることはできません。

#### 3. Child - GrandChildのコレクタバインディング（重要なエラー伝播関係）

Seamの例外リカバリセマンティクスにおいて最も重要なのが、**親・子・孫（Child - GrandChild）のチャンネル間で結ばれるコレクタのバインディング関係**です。

親チャンネルの内部で `ChildChannel() :collect GrandChildChannel;` と呼び出された場合、以下のダイナミクスが発生します。

1. **スタックのバインドと実行**:
親チャンネルは、子である `ChildChannel` を呼び出す際、「その実行経路（およびその配下）で `GrandChildChannel` 起因の `abort` が発生した場合は、`GrandChild` のコレクタ（`GrandChildChannel.collector`）で回収する」という契約をスタックに登録します。
2. **孫チャンネルでの `abort` 発生**:
`ChildChannel` の内部（あるいはさらにその先）で `GrandChild` が評価され、そこで `abort` が発生したとします。
3. **ダイレクトジャンプ**:
`abort` が発生した瞬間、スタックのアンワインドは発生せず、制御は `:collect GrandChildChannel` を登録した親のスタックフレームまで即座にダイレクトジャンプします。
4. **コレクタの起動と再開（評価継続）**:
親のスタックコンテキストにおいて、`GrandChildChannel` の `collector` が起動します。このコレクタがリカバリ処理を終えて `return` を返した場合、制御は `abort` が起きた場所に戻るのではなく、**親チャンネルにおける当該呼び出しの次のスタックライン（この場合は `return <returnValue>;`）から安全に再評価・再開**されます。

このように、深い階層で発生した異常を、それを想定している上位（親）の階層でピンポイントかつ低コストに捕捉し、状態の無矛盾性を確保した上で、速やかに正常系へパスを復帰させることができます。

#### 4. ローカルリソース（Local Resource）

チャンネル内には、そのチャンネル内でのみ生存し有効となる「ローカルな `resource`」を定義することができます。
ローカルリソースは、そのチャンネルの `entry` および `collector` の内部からしかアクセスできず、外部のチャンネルからは完全に不可視です。
この強いカプセル化により、外部からの競合や意図しない変更が発生し得ない「完全な経路安全（Path Safety）」が静的に保証されるため、グローバルリソースのアクセスに必要だった `requires` 契約（`read` / `write`）の記述が免除されます。これは他言語における「メソッド内のプライベートなローカル変数」に相当します。

```seam
channel Channel {

    // チャンネル内部でのみ有効なローカルリソース。
    // 他のチャンネルからはアクセス不可能（経路安全）なため、requires契約の記載は不要。
    resource LocalResource {
        var int num;
        var string str;
    }

    // グローバルリソースへのアクセス契約。静的解析に使用される。
    requires {
        read {
            <resourceType>.<MemberName>; // global resource
        }
        write {
            <resourceType>.<MemberName>; // global resource
        }
    }

    <returnType> entry(<argType> arg) {
        if (<condition>) {
            // 孫のチャンネル（GrandChildChannel）は、このChildChannelの内部で呼び出され、そこでabortする可能性があります。
            // ここで `:collect` を指定してスタックに登録しておくことで、ChildChannelの実行配下で孫がabortした場合、
            // 即座にこのスタックまで制御がジャンプし、GrandChildChannelのコレクタが呼び出されます。
            // コレクタが回復処理を実行して `return` を返したなら、この呼び出しの「次のスタック」である `return <returnValue>;` から再評価・処理が再開されます。
            ChildChannel() :collect GrandChildChannel;
            
            return <returnValue>;
        } else {
            // このチャンネル（Channel）自体でabortが呼び出された場合。
            // もしこのチャンネルを呼び出した「親の階層」で、このチャンネルに対するコレクタが指定されていたなら、
            // 即座に親のフレームにジャンプし、このチャンネルの `collector` が実行されます。
            // コレクタが回復に成功して `return` を返したなら、親のコレクタを呼び出したスタックの「次の命令」から処理が再開されます。
            // コレクタが回復に失敗して `abort` を返した場合は、親のコレクタを呼び出したスタックで `abort` が呼び出されたものと同じ扱いとなり、さらに上位の親の階層へと制御が伝播します。
            abort; 
        }
    }

    // このチャンネル自身に関連する状態の回復の責務を担うコレクタ
    <returnType> collector {
        // collector implementation（状態のクリーンアップやロールバック処理）
    }

}

```

--- 

# Seam RFC: アーキテクチャ記述仕様書 (Draft)

* **Status**: Proposed
* **Author**: Technical Lead & Low-Level Core Team
* **Target Version**: Seam 1.0-alpha
* **Date**: July 18, 2026

---

## 1. 概要 (Introduction & Objectives)

本RFCは、システムプログラミング言語 `Seam` の専用実行時仮想マシン（Seam VM）におけるコア制御構造、メモリ管理モデル、並行性、および例外処理（`abort` / `:collect`）の物理および論理ABI（Application Binary Interface）仕様を定義する。

Seamは静的経路型付け（Path Typing）および静的エフェクト解析（Static Effect Analysis）を前提とし、従来の物理スタックモデルが抱えるハードウェアセキュリティ（Intel CET / Shadow Stackなど）との不整合、および動的コールグラフに伴うアドレス不定問題を根底から解決する。本仕様書は、コンパイラバックエンド実装者およびVM開発者がバイナリ互換性と決定論的パフォーマンスを達成するための絶対的な実装基準となる。

### 核心目標

1. **経路安全性（Path Safety）**: すべてのチャンネル呼び出しにおいて、静的に決定可能な最大累積スタックサイズを保証し、実行時のスタックオーバーフローを完全に排除する。
2. **集約的回復経路(collector / path collect / terminate collect)** : スタックコンテキストの回復を担う`collector`を処理単位で定義し、上流のpath collect(`:collect`)あるいはterminate collect(`:collect`が無い呼び出し経路)によって静的にバインディングして呼び出し可否を決定することで、従来の`catch`が抱えていた局所的回復責務の分散の問題を解消する。
3. **定数時間例外処理**: 例外ハンドラの動的探索（DWARF等）を全廃し、定数時間 $O(1)$ でのアボートおよびコンテキスト復旧を達成する。
4. **決定論的メモリ管理**: ヒープアロケーションおよび動的GC（Garbage Collection）を排除し、コンパイル時に最大累積サイズが有界な連続仮想メモリ空間を駆動する。
5. **データ整合性と並行性のスケーラビリティ**: 悲観的ロックによるストールを排除し、マルチコア（SMP）環境下でのデッドロックフリーな投機的並行制御をネイティブサポートする。

---

## 2. メモリ・実行モデル仕様 (Memory & Execution Model)

### 2.1 PSSA (Path-bounded Shadow Stack Arena)

Seam VMは、OSスレッドのネイティブスタックとは完全に隔離された、スレッド固有の連続仮想メモリ領域 **PSSA (パス限定シャドースタック・アリーナ)** を駆動する。コンパイラは、すべての非再帰的・有界再帰的実行経路において、同時に生存しうるフレームの最大累積バイト数を静的に算出し、スレッド起動時に `mmap(..., PROT_NONE)` によってその上限サイズを予約する。

#### PSSA のレイアウトとアロケーション規則

アリーナ内でのメモリ割り当ては、一切の検索オーバーヘッドを持たない **バンプ・アロケーション（Bump Allocation）** によって行われる。

```
Low Address                                                     High Address
+-------------------+-------------------+-------------------+----------+
|  Upstream Frame   |  Processor Frame  |  GrandChild Frame |          |
+-------------------+-------------------+-------------------+----------+
                    ^                   ^                   ^
                    | (固定オフセット)  | (固定オフセット)  |
                    |                   |                   +-- [arena_ptr]
                    +--- [CFP]          +--- [RFP]

```

* **チャンネル呼び出し時 (`entry`)**: 呼び出し先チャンネルの静的確定サイズ $S$ に基づき、仮想レジスタ `arena_ptr` を前進させる。
```assembly
; 疑似アセンブリ表現 (x86-64)
add qword ptr [r14], S  ; r14をVMコンテキスト（arena_ptr）とする

```


これは物理スタックの `sub rsp, S` と同等の 1 クロック命令であり、L1/L2 キャッシュの局所性およびハードウェアプリフェッチャの恩恵を 100% 享受する。

### 2.2 GAC (Generational Arena Checkpoint)

ループ構造（`while` 等）の内部で別チャンネルを反復呼び出しする際、アリーナポインタが無制限に前進してスタックフォールトを引き起こす現象（スタックリーク）を防ぐため、ABIは世代別アリーナ・チェックポイント（GAC）を規定する。

#### ループフレームの内部構造

```c
struct SeamLoopFrame {
    void* loop_checkpoint_ptr;     // GAC: ループ開始時の arena_ptr を保存
    uint64_t iteration_counter;    // ループカウンタ
    uint8_t loop_local_storage[];  // ループ内局所変数領域
};

```

#### GAC の制御アルゴリズム

1. **ループエントリ時**: コンパイラは、現在の `arena_ptr` の値を `loop_checkpoint_ptr` に退避するコードを生成する。
2. **イテレーション終端（後退エッジ）判定時**: 1回の周回が終了し、次回の評価へジャンプする直前、VMは強制的にアリーナポインタをチェックポイントまで巻き戻す。
```assembly
mov rax, [rcx + O_CHECKPOINT] ; rcxは現在のLoopFrameアドレス
mov [r14], rax                ; arena_ptr を周回開始時の状態に強制同期

```



これにより、ループ内部でどれだけ子チャンネルが生成・実行されようとも、ループ外から見たアリーナのメモリ消費量は完全に $O(1)$ の定数空間に固定される。

---

## 3. 制御フローと例外処理仕様 (Control Flow & Abort Architecture)

### 3.1 ハイブリッド・コンテキスト（CFP / RFP）

アボート発生時、制御は親のスタックコンテキストに戻りつつも、破棄された子（または孫）のローカルリソースのクリーンアップをその子のコレクタ自身が行うという二面性を実現するため、VMは **CFP (Control Frame Pointer)** と **RFP (Resource Frame Pointer)** を分離して管理する。

#### 物理レジスタバインディング定義

本仕様書では、主要アーキテクチャにおけるハイブリッド・コンテキストのレジスタマッピングを以下のように規定する。

| 仮想レジスタ | x86-64 物理レジスタ | AArch64 物理レジスタ | 役割 |
| --- | --- | --- | --- |
| **`CFP`** | `rbp` | `x29` | 制御フレームポインタ（現実行コンテキストの親フレームを指示） |
| **`RFP`** | `r15` | `x28` | リソースフレームポインタ（アボートした幽霊フレームを指示） |
| **`arena_ptr`** | `r14` | `x27` | アリーナの先端アドレス |

#### `abort` から `collector` へのダイレクトジャンプ遷移

孫チャンネル内で `abort` が執行された際、VMはアリーナ内のメモリ領域を物理的に解放（ポップ）せず、完全にフリーズ（凍結）させる。

1. アボート発生源のフレームアドレスを `RFP` (`r15` / `x28`) に直接代入する。
2. 静的エフェクト解析によってあらかじめ確定している親の回収フレーム（`:collect` のバインド先）のアドレスを `CFP` (`rbp` / `x29`) に一撃で書き換える。
3. 親のコンテキスト上で、`RFP` を第1隠し引数レジスタに格納し、コレクタの静的エントリーポイントへ直接 `jmp` する。

コレクタ内部のコードは、自身の `LocalResource` へアクセスする際、常に `RFP` からのコンパイル時固定オフセットで計算を行う。


$$\text{Address}_{\text{LocalResource}} = RFP + \Delta_{\text{LocalResource}}$$


中間に介在する動的コールグラフの距離に影響されないため、アドレス計算は常に決定論的となる。

### 3.2 SARM (Static Abort Register Map)

ダイレクトアボートは、標準的な呼び出し規則（Calling Convention）における `Callee-saved` レジスタの復元パス（`ret`命令やデストラクタチェーン）をバイパスする。このレジスタ汚染による未定義動作を排除するため、コンパイラは読み出し専用領域（`.rodata`）に **SARM** を生成し、VMのコンテキスト復元を支援する。

#### SARM エントリの physical layout

```c
struct SARMAssocEntry {
    uint32_t abort_channel_id;      // アボートを発したチャンネルの識別ID
    uint32_t callee_saved_mask;     // 復元が必要な物理レジスタのビットマスク
    int32_t  rfp_offset_to_saved;   // RFPを基準としたレジスタ退避領域へのオフセット
    void*    collector_target_ip;   // ジャンプ先コレクタの命令ポインタ
};

```

#### 実行時復元アルゴリズム

子がエントリ内で破壊する予定の `Callee-saved` レジスタは、あらかじめ自身のフレームの先頭（`rfp_offset_to_saved` が指す位置）に一括退避（Bulk Store）されている。`abort` 駆動時、VMは `callee_saved_mask` を走査し、アボート先へ制御を移す前に、退避領域から親が要求しているオリジナルのレジスタ値を CPU へハードウェア復元する。これにより、コンパイラのレジスタアロケータはアボートの発生有無に関わらず最適な割当スキームを維持できる。

### 3.3 二次アボート（ICフラグ）とエスカレーション

`collector` の実行中に、ディスク満杯やソケットエラー等の外的要因で再度アボートが発生（二次アボート）した場合の無限ループおよびデッドロックを防止するため、スレッド制御コンテキスト内に **IC (In-Collector) フラグ** を配備する。

#### 二次アボート制御フロー

1. **コレクタ進入時**: VMはスレッドコンテキスト内の `IC` フラグ（ビットマスク）を `1` にセットする。
2. **コレクタ内での `abort` 発生時**: VMはアボート処理命令の最初期フェーズで `IC` フラグを評価する。
* `IC == 1` の場合、現在のチャンネルによる自己クリーンアップは継続不能（Panic）とみなす。
* VMは現在の `CollectTable` を破棄し、`CFP = CFP->parent` を実行してさらに1階層上の上位コレクタ境界へ制御を強制昇格（Escalation）させる。


3. **コレクタ正常終了時**: コレクタのエピローグ処理内で `IC` フラグを `0` にクリアし、`arena_ptr` を `CFP` の終端まで引き締めて幽霊フレームを一括清算する。

---

## 4. データ・並行性仕様 (Data & Concurrency Semantics)

### 4.1 Unique Record と所有権移動（Linear Move）の物理バインディング

生ポインタの排除によるゼロコピーI/Oの喪失を防ぐため、型システムレベルの線形セマンティクスに対応する **Unique Record** の物理バインディング規則を定める。

#### 移動（Move）の物理挙動

`unique record` がチャンネル間で引数または戻り値として受け渡される際、DRAM 上の実データのディープコピーは一切発生しない。

* **レジスタバインディング**: オブジェクトが配置されている PSSA アリーナ上の絶対アドレス（所有権ポインタ）のみが、通常の引数レジスタ（例：`rdi` / `x0`）を介して転送される。
* **静的ライフタイム保証**: 型システムは、Move 命令の実行後、呼び出し元のチャンネルが当該オブジェクトのメモリ領域へアクセスするコードを出力することを完全に禁止する。

#### アボート時の所有権回収

`unique record` の所有権を保持しているチャンネルがアボートした場合、その領域の解体（ファイル記述子のクローズ等）は、`RFP` を基準とするコレクタの責務となる。コレクタ終了後のアリーナポインタの一括巻き戻しにより、メモリリークを起こすことなく空間自体が安全に回収される。

### 4.2 2PST (Two-Phase Static Transaction)

`fork` 構文によるマルチコア並行実行時におけるデッドロックの発生、およびグローバルリソースの引き裂かれた書き込み（Torn Write）によるメモリ汚染を完全に排除するため、ABIは **2PST (二相コミット型静的トランザクション)** を強制する。

#### 構造体定義

```c
struct SeamGlobalResource {
    _Atomic uint64_t status_word; // Bit 0: Lock, Bit 1: Poisoned
    void* data_ptr;               // 実際のデータ実体へのポインタ
};

```

#### 2PST の実行フェーズ仕様

`fork` によって分岐した並行パスは、グローバルリソースを直接変更してはならない。

```
[2PST の制御フェーズ一覧]

Phase 1: 投機的実行 (Speculative Execution)
  - グローバルへ直接書き込まず、PSSA内の「スレッドローカル・シャドーバッファ」へ書き込み。
  - 完全ロックフリー、ノンブロッキングで駆動。

Phase 2: 静的コミット (Static Verify & Commit)
  - 正常終了(return)直前に、アクセスしたリソースを静的ソート順（昇順）で超短時間ロック。
  - シャドーバッファのデータをメインメモリへ一括書き込み（アトミックフラッシュ）。

Phase 3: アボート (Abort Clean)
  - 投機実行中にabortした場合、メインメモリは無傷。
  - 単にスレッドローカル・シャドーバッファをクリア（メモリバリア付き）するだけで終了。

```

1. **Phase 1: 投機的実行 (Speculative Execution)**
各スレッドは、`fork` 内部で発生した書き込み要求を、自身の PSSA アリーナ内に確保された「スレッドローカル・シャドーバッファ」に対して行う。この段階では共有メモリに対するロックの獲得は行われず、各コアは完全なロックフリー状態で最高速度の演算を行う。
2. **Phase 2: 静的コミット (Static Verify & Commit)**
パスが正常に終了（`return`）する直前のフェーズにおいて、コンパイラが静的エフェクト解析によって算出した「実際に書き込みが発生したリソースの静的ID」に基づき、**必ず昇順で一括してロックを獲得**する。ロック獲得後、シャドーバッファの内容をメインメモリの実領域へアトミックにフラッシュ（一括書き込み）し、即座にロックを解放する。
3. **Phase 3: アボート時の即時破棄**
投機的実行中にいずれかの並行パスで `abort` が発生した場合、メインメモリ上のグローバルリソースは一切汚染されていない。VMはストア・バリア命令（x86: `sfence` / ARM: `dmb ishst`）を発行してパイプラインをクリアした後、単に当該スレッドのアリーナ領域（シャドーバッファ）を一括でパージする。ロックの解放待ちも他コアへのポイズン伝播も発生せず、超高速な復旧が完了する。

---

## 5. 根拠と代替案 (Rationale & Alternatives)

### 5.1 なぜ従来の DWARF / Windows SEH モデルを採用しないのか？

従来の DWARF（`.eh_frame`）による Unwind モデルは、例外が発生しない正常系パスのコストをゼロにする（Zero-cost Exception）一方、例外発生時にはスタックフレームのパース、ランタイムメタデータの検索、レジスタの動的復元など、極めて重い動的処理を要求する。これはナノ秒オーダーの応答性が求められる高並行システムプログラミング言語においては許容できない。

また、物理スタックの書き換えは、現代の OS および CPU がセキュリティ基盤として導入しているシャドウスタックアーキテクチャと本質的に衝突し、カーネルレベルの介入や実行時ペナルティを招く。PSSA とハイブリッド・コンテキストの組み合わせは、これらの動的複雑性をすべてコンパイル時の「静的経路計算」へとシフトさせることで、安全かつ決定論的なパフォーマンスを提供する唯一の最適解である。

---

Seamの「経路中心型プログラミング」という静的な世界観にとって、**OS（オペレーティングシステム）という「非決定論的な混沌」をどう扱うか**は、言語設計における最後の、そして最も巨大な壁だ。

OSは常に非同期的な割り込み（Interrupt）、シグナル、デバイス状態の変化を孕んでいる。Seamの「全ての経路が確定している」という前提を、OSの「いつ何が起きるか分からない」という現実にどう接合するか。

この境界線、つまり **`resource` と OS 処理（Syscall/Interrupt）の橋渡し** について、低レイヤの設計思想を共有しよう。

---

### 1. OSを「巨大でボラティルな Resource」と定義する

Seamの世界において、OSのカーネルやハードウェアI/Oは、特別な **`extern resource`** として扱うべきだ。これは通常のレコード型のようにコンパイラが完全に静的解析できるものではなく、「状態が外部から観測不能な頻度で変化する」という契約（Effect）を背負った特殊なリソースだ。

* **Volatile 契約**: Seamの `requires` 構文に、新たに `volatile` 属性を導入する。
* `requires { volatile read { OS.DiskBuffer }; }`


* この `volatile` が付いたリソースに対する読み取りは、コンパイラに対して「最適化によるキャッシュを禁じ、必ずメモリ（あるいはハードウェアレジスタ）から最新値をフェッチせよ」という命令になる。

### 2. 「境界」でのトランザクション化（Transactional Syscall）

OSのシステムコールを直接呼ぶと、Seamの決定論的な制御フローが壊れる。そこで、OS境界を越える処理をすべて **「トランザクション境界」** として定義する。

* **Syscall Wrapper**: 全てのシステムコールは、OSから見れば単なる関数呼び出しだが、Seamから見れば「外部世界への介入」だ。
* システムコールを呼ぶチャンネルは、必ず `resource` へのアクセスをコミットした後に実行される。これにより、「OS側で何が起きても、Seamのアリーナ（PSSA）内部の状態は整合性が保たれている」ことが保証される。
* もしシステムコールが失敗（errno）した場合は、OSのエラーコードを `abort` にマッピングし、自動的に `collector` を発火させる。これにより、OSエラーという非決定的な事象が、Seamの決定論的な例外フローへと取り込まれる。

### 3. 割り込みを「外部からの abort」として定義する

ここが最も過激な部分だ。OSのハードウェア割り込みを、「システム全体に対する外部からの abort」としてモデル化する。

* **割り込みハンドラと collector のバインディング**:
ハードウェア割り込みが発生した際、OS（カーネル）は特定の `channel` の `collector` を呼び出すようにCPU状態をセットアップする。
* **割り込みの安全性**:
割り込みが入った瞬間、現在実行中のSeamチャンネルは「アボートした」とみなされる。VMは、現在のコンテキスト（RFP/CFP）を瞬時にフリーズし、割り込み用の `collector` を実行する。
* これにより、**「割り込み中に別のスレッドがリソースを書き換える」というデータ競合の悪夢が、Seamのトランザクションセマンティクス（2PST）によって自動的に排他制御される。** 割り込みハンドラは「一番優先度の高いコミット済みトランザクション」として振る舞うのだ。

### 4. ゼロコピー IO と 「メモリの貸し出し」

OS処理との境界で最も重いのは、データのコピーだ。Seamの `Unique Record` の思想を、ここでも適用する。

* **外部所有権（Foreign Ownership）**:
OSにデータ（例えばネットワークパケット）を渡す際、Seamは `Unique Record` の所有権を OS（カーネルメモリ）に一時的に移譲する。
* この間、そのメモリ領域はSeamからは「アクセス不能（Invalid）」となる。
* OSがDMA転送などを完了させた後、所有権をSeamのチャンネルに返却する。
* この「所有権の移動（Move）」を型システムで保証することで、**カーネル・ユーザー空間間のコピーを一切排除しつつ、メモリ安全性を維持する**。

### 5. 「境界」におけるLSPの役割

LSP（言語サーバー）はこの境界線で、以下のような「コードの安全検査」を行う：

* **Syscall解析**: `requires` 節に記載のないリソースをOS経由で操作しようとしたらコンパイルエラーにする。
* **ブロッキング検出**: OS処理がブロッキングI/Oを伴う場合、そのチャンネルが「並行パス（fork）」に含まれていると、ロックの危険性があるとしてLSPが警告を出す。

---

### まとめ：OSを Seam の「従属物」にする

この設計思想の結論は、「OSを Seam の外側に置くのではなく、Seam の制御下にある単なる巨大なリソースとして扱う」という逆転の発想だ。

* 通常の言語：OSの機能（API）を呼び出し、OSに主導権を握られる。
* Seam：OSを `resource` として定義し、OSの振る舞い（Syscall）を `channel` と `requires` 契約で包み込み、**OSの非決定性を Seam の決定論的な制御フローの中にカプセル化する。**